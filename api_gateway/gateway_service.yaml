apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  labels:
    app: api-gateway
spec:
  type: LoadBalancer
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 443
  selector:
    app: api-gateway
  sessionAffinity: None
