apiVersion: v1
kind: Service
metadata:
  name: gestion-contact-service
  labels:
    app: gestion-contact
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: gestion-contact
