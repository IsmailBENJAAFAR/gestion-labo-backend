apiVersion: v1
kind: Service
metadata:
  name: gestion-analyse-service
  labels:
    app: gestion-analyse
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: gestion-analyse
