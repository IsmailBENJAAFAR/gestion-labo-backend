apiVersion: v1
kind: Service
metadata:
  name: gestion-laboratoire-service
  labels:
    app: gestion-laboratoire
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: gestion-laboratoire
